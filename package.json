{
  "name": "localshare",
  "version": "1.0.0",
  "description": "⚡ Ultra-fast WiFi file sharing — no apps, no cloud, no accounts.",
  "main": "server/index.js",
  "bin": { "localshare": "./server/index.js" },
  "scripts": {
    "start":        "node server/index.js",
    "dev:server":   "node server/index.js",
    "dev:client":   "cd client && npx vite",
    "dev":          "start \"LocalShare Server\" cmd /k npm run dev:server & start \"LocalShare Client\" cmd /k npm run dev:client",
    "build":        "cd client && npx vite build",
    "install:all":  "npm install && cd client && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^2.1.1",
    "open": "^9.1.0",
    "qrcode": "^1.5.3",
    "ws": "^8.14.2"
  },
  "keywords": ["file-sharing","wifi","local-network","lan","transfer"],
  "license": "MIT",
  "engines": { "node": ">=16.0.0" }
}
